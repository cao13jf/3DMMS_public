function D = vigraFunctionDistanceTransform(vol)
% function D = vigraFunctionDistanceTransform(vol)
%  
% D = vigraFunctionDistanceTransform(f) computes the Euclidean distance transform for given function f.
% 
% f - 1D/2D/3D input array
% 
% Usage:
% D = vigraFunctionDistanceTransform(vol);
% 
  error('mex-file missing. Call buildVigraExtensions(INSTALL_PATH) to create it.')