function [labels] = vigraMaxFlow(unary, pair)
% function [labels] = vigraMaxFlow(unary, pair)
%  
% [labels] = vigraMaxFlow(unary, pair) performs max-flow with given unary and pair potentials.
% 
% Usage:
% [labels] = vigraMaxFlow(unary, pair);
% 
  error('mex-file missing. Call buildVigraExtensions(INSTALL_PATH) to create it.')